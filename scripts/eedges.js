'use strict';


eedges = new Array

class Eedges {
  constructor(edges){
    this.edges = edges
  };
}
enodes.import = function(json_nodes){
  json_nodes.forEach(n => {
    node = new Enode(n)
    this.set(n.id, node)
  })
  return this
}

Enodes = enodes
module.exports = Eedges;

